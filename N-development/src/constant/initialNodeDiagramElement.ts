import {EColor, EFontColor} from "./index";
import {IDiagramNodeStyle} from "../interface";

export const initialNodeDiagramElement: IDiagramNodeStyle  = {
    textStyles: {
        fontFamily: 'Inter',
        fontSize: 14,
        fontColor: EFontColor.grey4,
    },
    borderWidth: 1,
}
